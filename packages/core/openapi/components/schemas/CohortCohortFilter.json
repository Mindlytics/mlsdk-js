{
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "cohort"
      ]
    },
    "name": {
      "type": "string",
      "description": "The name of the cohort.  Can be \"All Users\" to match all users."
    }
  },
  "required": [
    "type",
    "name"
  ]
}